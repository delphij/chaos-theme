{{- $pages := where site.RegularPages "Type" "in" site.Params.mainSections -}}
{{- $limit := site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
{{ "<?" | safeHTML }}xml-stylesheet type="text/xsl" href="{{ "atom.xsl" | relURL }}"{{ "?>" | safeHTML }}
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ site.Language.Locale }}">
  <title>{{ site.Title }}</title>
  {{- with site.Params.subtitle }}
  <subtitle>{{ . }}</subtitle>
  {{- end }}
  <id>{{ "/" | absLangURL }}</id>
  <author>
    <name>{{ site.Title }}</name>
    <uri>{{ "/" | absLangURL }}</uri>
  </author>
  <generator>Hugo gohugo.io</generator>
  {{- with site.Copyright }}
  <rights>{{ . }}</rights>
  {{- end }}
  {{- with site.Params.brand.icon }}
  <icon>{{ . | absURL }}</icon>
  {{- end }}
  {{- with site.Params.brand.logo }}
  <logo>{{ . | absURL }}</logo>
  {{- end }}
  <updated>{{ now.UTC.Format "2006-01-02T15:04:05Z" | safeHTML }}</updated>
  {{- with .OutputFormats.Get "ATOM" }}
  {{ printf `<link rel="self" type="%s" href="%s" hreflang="%s"/>` .MediaType.Type .Permalink site.Language.Locale | safeHTML }}
  {{- end }}
  {{- range .AlternativeOutputFormats }}
  {{ printf `<link rel="alternate" type="%s" href="%s" hreflang="%s"/>` .MediaType.Type .Permalink site.Language.Locale | safeHTML }}
  {{- end }}
  {{- range first 25 $pages }}
  <entry>
    <title>{{ .Title }}</title>
    <author>
      <name>{{ or site.Params.author.name site.Title }}</name>
      <uri>{{ or site.Params.author.uri (absLangURL "/") }}</uri>
    </author>
    <id>{{ .Permalink }}</id>
    <link rel="alternate" type="text/html" href="{{ .Permalink }}"/>
    {{- if .IsTranslated -}}
    {{ range .Translations }}
    <link rel="alternate" href="{{ .Permalink }}" hreflang="{{ .Language.Lang }}"/>
    {{- end -}}
    {{ end }}
    <updated>{{ .Lastmod.UTC.Format "2006-01-02T15:04:05Z" | safeHTML }}</updated>
    <published>{{ .Date.UTC.Format "2006-01-02T15:04:05Z" | safeHTML }}</published>
    <summary type="html">{{ trim .Summary "\n" }}</summary>
    <content type="html" xml:base="{{ "/" | absLangURL }}">{{ "<![CDATA[" | safeHTML }}{{ .Content }}{{ "]]>" | safeHTML }}</content>
  </entry>
  {{- end }}
</feed>
